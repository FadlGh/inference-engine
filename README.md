# Gym Assistant

Requirements: `mediapipe`, `opencv-python`, `numpy`

Install:

    pip install mediapipe opencv-python numpy

Run:

    python pushup_analyzer.py
    # or pass a video path
    python pushup_analyzer.py myvideo.mp4
